# CLAUDE.md — AI Assistant Guide for toolkit-phase1

## Project Overview

**toolkit-phase1** is a new project repository in its initial setup phase. No application code, build system, or dependencies have been established yet.

- **Repository**: toolkit-phase1
- **Default branch**: master
- **Status**: Early-stage — scaffolding and initial development

## Repository Structure

```
toolkit-phase1/
├── CLAUDE.md          # This file — AI assistant guide
├── README.md          # Project readme (minimal)
└── .git/              # Git metadata
```

No source code, configuration files, or dependencies exist yet. As the project grows, update this section to reflect the actual structure.

## Development Setup

No build system or package manager is configured. When one is added, document:

- Language and runtime version requirements
- Package manager (npm, yarn, pnpm, etc.)
- Install command (e.g., `npm install`)
- Environment variable setup

## Build & Run

No build scripts exist yet. When added, document:

- Build command
- Dev server command
- Production build command

## Testing

No test framework is configured. When added, document:

- Test framework (Jest, Vitest, pytest, etc.)
- How to run tests (e.g., `npm test`)
- Test file naming conventions and locations
- Coverage requirements

## Linting & Formatting

No linters or formatters are configured. When added, document:

- Lint command (e.g., `npm run lint`)
- Format command (e.g., `npm run format`)
- Pre-commit hooks if any

## Code Conventions

No conventions have been established yet. As code is written, document:

- Naming conventions (files, functions, variables, classes)
- Module/import patterns
- Error handling patterns
- Logging conventions

## Git Workflow

- **Primary branch**: master
- Write clear, descriptive commit messages
- Keep commits focused on a single logical change

## Key Reminders for AI Assistants

- Read existing code before proposing changes
- Do not over-engineer — keep solutions minimal and focused on the task
- Do not add features, refactoring, or improvements beyond what is requested
- Avoid introducing security vulnerabilities (injection, XSS, etc.)
- Update this CLAUDE.md file when significant project structure or workflow changes are made
